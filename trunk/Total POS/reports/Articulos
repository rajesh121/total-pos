Title==Total de Articulos
Columns==Codigo,codigo,String|Marca,marca,String|Modelo,modelo,String|Descripcion,descripcion,String|Precio Venta,monto,bigDecimalType|Existencia Actual,existencia_actual,bigDecimalType,1
ShowNumbers==True
Parameters==Marca,,String,1|Codigo,,String,2|Modelo,,String,3
SQL==select distinct * from (select a.marca,a.modelo,a.codigo, a.descripcion, a.existencia_actual, round(p1.monto*1.12,2) as monto, p1.fecha from precio p1, articulo a where p1.fecha = (select max(p2.fecha) from precio p2 where p1.codigo_de_articulo=p2.codigo_de_articulo) and a.codigo = p1.codigo_de_articulo order by a.existencia_actual desc) as myTable where marca like concat(concat('%',trim(?)),'%') and codigo like concat(concat('%',trim(?)),'%') and modelo like concat(concat('%',trim(?)),'%')
Vertical==True
